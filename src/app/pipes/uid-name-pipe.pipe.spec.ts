import { UidNamePipePipe } from './uid-name-pipe.pipe';

describe('UidNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UidNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
